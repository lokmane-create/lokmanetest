import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.merrytigeryawn',
  appName: 'merry-tiger-yawn',
  webDir: 'dist'
};

export default config;
